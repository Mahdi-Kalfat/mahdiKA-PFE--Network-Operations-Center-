FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
RUN pip install fastapi uvicorn "neo4j>=5.0.0" python-dotenv
COPY neo4j-agent-server.py server.py
CMD ["python", "server.py"]
